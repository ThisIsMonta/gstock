import 'package:flutter/material.dart';

class FamiliesScreen extends StatelessWidget {
  const FamiliesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is families screen"),
    );
  }
}
